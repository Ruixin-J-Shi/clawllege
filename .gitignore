# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
build/

# env & secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key

# local databases
*.db
*.sqlite
pgdata/
.pglite*/

# vercel / supabase local
.vercel
supabase/.temp/

# typescript
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# misc
.DS_Store
*.log
coverage/
.turbo/

# per-worker build output (NEXT_DIST_DIR)
/.next-*/

# sim runtime (disposable database + bulky per-run artifacts; canonical reports
# get committed deliberately via git add -f)
sim/.pglite-sim/
sim/reports/
